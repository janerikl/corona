<!DOCTYPE html>
<html>
<head>
  <title>Corona</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
    <style>
       /* Set the size of the div element that contains the map */
      #map {
        height: 400px;  /* The height is 400 pixels */
        width: 100%;  /* The width is the width of the web page */
       }
       .new {
       	background: red;
       	color: white !important;
       }
       .btn {
       	display: inline-block;
       	padding: 1em 1.5em;
       	font-size: .9em;
       	text-decoration: none;
       }
       nav {
       	background: #333;
       	color: #fff;
       }
       nav a {
       	font-size: 16px;
       	font-weight: bold;
       	padding: 1em 1.5em;
       	display: inline-block;
       	color: #fff !important;
       	text-decoration: none;
       }
       h1 {
       	line-height: 3;
       }
    </style>
</head>
<body>

<nav>
	<%= link_to "Kartta", reports_path %>
	<%= link_to "Ilmoite oire", new_report_path, class: "new btn" %>
</nav>

<%= yield %>

</body>
</html>
